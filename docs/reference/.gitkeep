# The one live reference set (Bar A: A10).
# Put how-it-works-TODAY notes here, one doc per subsystem. Deleted designs live in git history, never as
# live-looking docs. Remove this .gitkeep once the first reference doc lands.
