# Glossary — the ubiquitous language of this engagement

<!--
  A HANDOVER HOME (exit-package gate row 5.2). It ships empty on purpose: the gate asks
  whether the home EXISTS, and you fill it as you build, not in a scramble the week of
  handover. Delete nothing; add rows as the vocabulary settles.

  WHY THIS EXISTS. The receiver inherits your code and your words. A term the client's
  business uses ("active account", "returned unit", "cycle") almost never means exactly
  what the same word means in the schema, and every place those two drift is a place the
  receiver will misread the system. This file is where you pin the mapping ONCE so the
  code, the deliverables, and the client's own meetings can use one vocabulary.

  RULE: a term belongs here when the business word and the code word are not obviously
  the same thing, or when getting it wrong would change a number someone reports. Do not
  pad it with terms whose meaning is self-evident from the identifier.
-->

**Engagement:** `<slug>`  ·  **Maintained by:** the solutioner-of-record  ·  **Updated:** YYYY-MM-DD

| Term | What the business means by it | Where it lives in the system | Notes / gotchas |
|---|---|---|---|
| `<Business term>` | `<the definition a client stakeholder would give>` | `<table.column, service, or module>` | `<the mismatch that bites — units, timezone, inclusive/exclusive, who owns it>` |

## Terms we deliberately do NOT use

<!-- Words the client's org uses ambiguously, and what we say instead. This section
     prevents a receiver from "helpfully" reintroducing the ambiguous term later. -->

| Avoid | Say instead | Because |
|---|---|---|
| `<ambiguous term>` | `<the precise term>` | `<the two things it was being used for>` |
